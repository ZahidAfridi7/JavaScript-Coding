str = "hello world"
//JS strings are immutable! we can not make chnages directly in string

//String built-in function

str = str.toUpperCase()
str = str.toLowerCase()
str = str.trim()

str = str.slice(0,5) //(start,end)
str2 = 'welcome'
str = str.concat(str2) // join two strings

str = str.replace('ll', 'hh') //first search,2nd replace

str = str.charAt(0) // search for char at index 0
